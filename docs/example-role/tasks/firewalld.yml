- name: setup EXAMPLE firewalld rules
  firewalld:
    zone: "{{ item[0].zone }}"
    service: "{{ item[1] }}"
    state: "{{ item[0].state }}"
    permanent: yes
    immediate: yes
  with_nested:
    - "{{ EXAMPLE_firewalld_zones }}"
    - ['service1', 'service2']
