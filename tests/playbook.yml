
- hosts: my.example.org
  roles:
    - common
    - backup
    - monitoring
    - apache
    - mariadb
    - postgresql
    - nextcloud
    - tt_rss
    - shaarli
    - gitea
    - samba
    - transmission
  # ignore_unreachable: yes # don't fail when host is unreachable
  # strategy: free # use this if your servers are independent from each other/do not require ordered/synchronized deployment
