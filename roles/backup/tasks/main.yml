- include: checks.yml
  tags:
    - backup
    - checks

- include: rsnapshot.yml
  become: yes
  tags: backup

- include: monitoring.yml
  become: yes
  tags:
    - backup
    - monitoring
  when:
    - '"nodiscc.xsrv.monitoring" in role_names'
