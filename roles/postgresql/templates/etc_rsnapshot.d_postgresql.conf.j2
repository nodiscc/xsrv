# rsnapshot configuration for postgresql backups
# dump all databases to /var/backups/postgresql/
backup_exec	/usr/local/bin/postgres-dump-all-databases.sh	required
# backup database dumps
backup	/var/backups/postgresql	localhost/
# remove temporary dumps
backup_exec	rm /var/backups/postgresql/*.sql	required
