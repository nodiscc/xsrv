#!/bin/bash

set -euo pipefail

# Configuration
MODELS_DIR="/var/lib/llamacpp/models"
MMPROJ_DIR="/var/lib/llamacpp/mmproj"
DOWNLOAD_LIST='{{ llamacpp_models | to_json }}'

# Parse download list
function parse_download_list() {
    echo "$DOWNLOAD_LIST" | jq -r '.[] | 
        if .mmproj_url then
            "\(.url)|\(.url | split("/") | last)|\(.mmproj_url)|\(.mmproj_url | split("/") | last)"
        else
            "\(.url)|\(.url | split("/") | last)||"
        end'
}

# Download function with aria2c
function download_model() {
    local url=$1
    local filename=$2
    local mmproj_url=$3
    local mmproj_filename=$4

    if [ -f "$MODELS_DIR/${filename}" ]; then
        echo "Model ${filename} already exists, skipping download"
    else
        echo "Downloading ${filename} from ${url}..."
        aria2c \
            --dir="$MODELS_DIR" \
            --out="${filename}" \
            --max-connection-per-server=16 \
            --split=16 \
            --min-split-size=1M \
            --file-allocation=none \
            --auto-file-renaming=false \
            --continue \
            --check-certificate=true \
            --retry-wait=5 \
            --summary-interval=60 \
            "${url}"
        echo "SUCCESS: $filename ($url)"
    fi

    # Verify download
    if [ -f "$MODELS_DIR/$filename" ]; then
        echo "Download verified"
    else
        echo "FAILED: $filename $url"
        return 1
    fi

    # Download mmproj if mmproj_url is set
    if [ -n "$mmproj_url" ]; then
        MMPROJ_SUBDIR="$MMPROJ_DIR/${filename%.*}"
        mkdir -p "$MMPROJ_SUBDIR"
        if [ -f "$MMPROJ_SUBDIR/${mmproj_filename}" ]; then
            echo "mmproj ${mmproj_filename} already exists, skipping download"
        else
            echo "Downloading mmproj for ${filename} from ${mmproj_url}..."
            aria2c \
                --dir="$MMPROJ_SUBDIR" \
                --out="${mmproj_filename}" \
                --max-connection-per-server=16 \
                --split=16 \
                --min-split-size=1M \
                --file-allocation=none \
                --auto-file-renaming=false \
                --continue \
                --check-certificate=true \
                --retry-wait=5 \
                --summary-interval=60 \
                "${mmproj_url}"
            echo "SUCCESS: mmproj for $filename ($mmproj_url)"
        fi

        if [ -f "$MMPROJ_SUBDIR/$mmproj_filename" ]; then
            echo "mmproj download successful for $filename"
        else
            echo "FAILED: mmproj download for $filename"
            return 1
        fi
    fi

    return 0
}

# Main execution
echo "Starting model downloads"
echo "----------------------------------------"

SUCCESS_COUNT=0
TOTAL_COUNT=0

while IFS='|' read -r url filename mmproj_url mmproj_filename; do
    TOTAL_COUNT=$((TOTAL_COUNT + 1))
    if download_model "$url" "$filename" "$mmproj_url" "$mmproj_filename"; then
        SUCCESS_COUNT=$((SUCCESS_COUNT + 1))
    fi
done < <(parse_download_list)

echo "----------------------------------------"
echo "Completed ${SUCCESS_COUNT}/${TOTAL_COUNT} models"

# Only exit with success if all models were either present or successfully downloaded
if [ $SUCCESS_COUNT -eq $TOTAL_COUNT ]; then
    exit 0
else
    exit 1
fi
