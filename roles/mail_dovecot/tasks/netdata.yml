- name: migration/1.16.0 - remove files from old versions of the role
  file:
    state: absent
    path: /etc/netdata/health.d/systemdunits.conf.d/dovecot.conf
  notify: assemble netdata configuration
